the output to this folder is produced by running the submit_stats_grid.sh script on a SLURM cluster (make a ./logs/ file first or change log folder in script)

It maps the parameter phase space of NPC transport 